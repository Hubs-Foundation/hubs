import React, { createContext, useCallback} from "react";
import PropTypes from "prop-types";
import { ChangeAvatarToolbarButton } from "./ChangeAvatarToolbarButton";  

  export function ChangeAvatarButtonContainer(props) {
    return <ChangeAvatarToolbarButton {...props} />;
  }
  