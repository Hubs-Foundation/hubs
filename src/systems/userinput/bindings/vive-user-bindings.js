import { paths } from "../paths";
import { sets } from "../sets";
import { xforms } from "./xforms";

// a default configuration to use when detecting vive controllers.
export const viveControllerUserBindings = {
};

// when we detect vive controllers, we expect keyboard and mouse(/trackpad)
// events are also available, so these bindings combine vive controller
// input bindings with some keyboard and mouse bindings.

